services:
  frontend:
    image: vohoangphuc/jobhuntly-admin-app:dev
    container_name: jobhuntly-admin-app
    restart: unless-stopped
    ports:
      - "5173:5173"
    networks:
      - jobhuntly-network
    pull_policy: always
    environment:
      - NODE_ENV=development
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5173"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  jobhuntly-network:
    external: true